---
export interface Props {
  title: string;
  description: string;
  image?: string;
  url?: string;
  type?: string;
}

const { title, description, image, url, type = 'website' } = Astro.props;
const canonicalUrl = url || Astro.url.href;
const base = import.meta.env.BASE_URL.replace(/\/$/, '') || '';
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="canonical" href={canonicalUrl} />

  <!-- Open Graph -->
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:type" content={type} />
  <meta property="og:url" content={canonicalUrl} />
  {image && <meta property="og:image" content={image} />}
  <meta property="og:site_name" content="Shopping With Noya" />

  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  {image && <meta name="twitter:image" content={image} />}

  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --bg: #0f172a;
      --surface: #1e293b;
      --text: #e2e8f0;
      --muted: #94a3b8;
      --accent: #60a5fa;
    }

    html { background: var(--bg); color: var(--text); font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; }

    /* Full-width nav stripe */
    .nav-stripe {
      background: #0f172a;
      border-bottom: 1px solid #1e293b;
      width: 100%;
    }
    .nav-inner {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 24px;
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .nav-brand { font-size: 1.15rem; font-weight: 700; color: var(--text); text-decoration: none; }
    .nav-brand span { color: var(--accent); }
    .nav-brand:hover { text-decoration: none; }
    .nav-cta {
      background: #0088cc; color: #fff !important; padding: 8px 18px;
      border-radius: 20px; font-size: 0.85rem; font-weight: 600;
      display: inline-flex; align-items: center; gap: 6px;
      text-decoration: none;
    }
    .nav-cta:hover { background: #0077b5; text-decoration: none; }
    .nav-cta.fb { background: #1877f2; }
    .nav-cta.fb:hover { background: #145db2; }
    .nav-links { display: flex; align-items: center; gap: 10px; }

    /* Page content wrapper */
    .page-body {
      max-width: 1200px;
      margin: 0 auto;
      padding: 32px 24px 0;
    }

    a { color: var(--accent); text-decoration: none; }
    a:hover { text-decoration: underline; }

    /* Footer */
    footer {
      margin-top: 60px;
      border-top: 1px solid #1e293b;
    }
    .footer-inner {
      max-width: 1200px;
      margin: 0 auto;
      padding: 24px;
      text-align: center;
      font-size: 0.8rem;
      color: var(--muted);
    }
    footer a { color: var(--muted); }
    .footer-links { display: flex; justify-content: center; gap: 20px; margin-bottom: 12px; flex-wrap: wrap; }
    .footer-links a { color: var(--accent); font-size: 0.85rem; }
    .disclaimer { font-size: 0.75rem; opacity: 0.65; margin-top: 8px; max-width: 600px; margin-left: auto; margin-right: auto; line-height: 1.5; }
  </style>
</head>
<body>

  <div class="nav-stripe">
    <div class="nav-inner">
      <a href={`${base}/`} class="nav-brand">🛍️ Shopping With <span>Noya</span></a>
      <div class="nav-links">
        <a href="https://www.facebook.com/groups/1224964507605101" class="nav-cta fb" target="_blank" rel="noopener">
          👥 Facebook Group
        </a>
        <a href="https://t.me/shoppingwithnoya" class="nav-cta" target="_blank" rel="noopener">
          📲 Join Telegram
        </a>
      </div>
    </div>
  </div>

  <div class="page-body">
    <slot />
  </div>

  <footer>
    <div class="footer-inner">
    <div class="footer-inner">
      <div class="footer-links">
        <a href="https://t.me/shoppingwithnoya" target="_blank" rel="noopener">📲 Telegram</a>
        <a href="https://www.facebook.com/groups/1224964507605101" target="_blank" rel="noopener">👥 Facebook Group</a>
      </div>
      <p>© {new Date().getFullYear()} Shopping With Noya</p>
      <p class="disclaimer">
        📌 Affiliate Disclosure: Some links on this site are affiliate links. If you click through and make a purchase,
        I may earn a small commission at no extra cost to you. I only share deals I genuinely think are worth your attention.
        Thank you for supporting ShoppingWithNoya! 🛍️
      </p>
    </div>
  </footer>

</body>
</html>
