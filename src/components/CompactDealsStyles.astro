<!-- Shared, page-scoped Option A treatment. Other routes keep their theme. -->
<style is:inline>
  .compact-deals { --compact-accent: #c4a2ef; --compact-solid: #7b2fbe; --compact-muted: #aaaac5; }
  .compact-deals :is(button, select, summary, a, input):focus-visible { outline: 3px solid #c4a2ef; outline-offset: 3px; }
  .compact-deals :is(button, select, summary) { font-family: inherit; }
  .compact-deals .compact-header { position: sticky; top: 0; z-index: 100; display: flex; justify-content: space-between; align-items: center; gap: 8px; height: 52px; padding: 0 12px; background: #16161f; border-bottom: 1px solid var(--border); backdrop-filter: none; }
  .compact-brand { color: var(--text); font-size: 18px; font-weight: 700; text-decoration: none; min-width: 0; letter-spacing: -.4px; }
  .compact-brand span { color: var(--compact-accent); }
  .compact-menu { position: static; flex-shrink: 0; }
  .compact-menu > summary { min-height: 44px; min-width: 44px; display: flex; gap: 8px; align-items: center; justify-content: center; cursor: pointer; list-style: none; border-radius: 8px; }
  .compact-menu > summary::-webkit-details-marker { display: none; }
  .menu-label { display: none; }
  .compact-menu-panel { position: absolute; top: 100%; right: 12px; width: min(360px, calc(100vw - 24px)); max-height: calc(100dvh - 64px); overflow-y: auto; padding: 12px; background: #1e1e2a; border: 1px solid #44445a; border-radius: 12px; box-shadow: 0 10px 30px #0008; }
  .compact-menu-panel > a { display: flex; align-items: center; min-height: 44px; padding: 8px 12px; color: var(--text); text-decoration: none; border-radius: 8px; white-space: normal; }
  .compact-menu-panel > a:hover { background: #303040; }
  .compact-menu-panel .nav-tg { background: var(--compact-solid); margin-top: 6px; font-size: .9rem; }
  .compact-menu-panel .nav-fb { background: #145db2; }
  .compact-menu-panel .guide-navigation { background: transparent; border: 0; }
  .compact-menu-panel .guide-navigation-inner { padding: 4px 12px; }
  .compact-menu-panel .guide-navigation a { min-height: 44px; display: flex; align-items: center; color: var(--compact-accent); }
  .compact-menu-panel .guide-navigation-inner > span { font-size: .8rem; }
  .compact-deals .search-wrap { max-width: 1200px; margin: 10px auto 8px; padding: 0 12px; z-index: auto; }
  .compact-deals #search-input { height: 44px; border-radius: 10px; border-width: 1px; background: #1e1e2a; }
  .compact-deals .search-clear { width: 44px; height: 44px; right: 12px; }
  .compact-deals .browse-controls { max-width: 1200px; margin: 0 auto 8px; padding: 0 12px; display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); gap: 8px; }
  .category-picker { position: relative; min-width: 0; }
  .category-picker > summary, .compact-deals #sort-order { width: 100%; min-height: 44px; border: 1px solid #44445a; border-radius: 10px; background: #1e1e2a; color: var(--text); font-size: .85rem; padding: 10px; cursor: pointer; }
  .category-picker > summary { overflow-wrap: anywhere; }
  .category-panel { position: absolute; top: calc(100% + 6px); left: 0; z-index: 50; width: min(440px, calc(100vw - 24px)); max-height: min(460px, 65dvh); overflow: auto; padding: 12px; border: 1px solid #44445a; border-radius: 12px; background: #1e1e2a; box-shadow: 0 10px 30px #0008; }
  .compact-deals .shop-cats { position: static; display: block; height: auto; margin: 0; padding: 0; background: none; border: 0; backdrop-filter: none; }
  .compact-deals .shop-cats::before, .compact-deals .shop-cats::after { display: none; }
  .compact-deals .shop-cats-scroll { display: flex; flex-wrap: wrap; gap: 8px; overflow: visible; padding: 0; mask-image: none; }
  .compact-deals .shop-cat { white-space: normal; overflow-wrap: anywhere; min-height: 44px; padding: 8px 10px; font-size: .82rem; }
  .compact-deals .category-links { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 14px; }
  .compact-deals .category-links a { display: block; padding: 10px; font-size: .8rem; color: var(--compact-accent); text-decoration: underline; }
  .compact-deals .category-links p { width: 100%; font-size: .8rem; color: var(--compact-muted); }
  .compact-deals .deals-section { padding: 0 12px 24px; }
  .compact-deals .section-header { display: flex; flex-wrap: wrap; align-items: center; gap: 4px 8px; min-height: 36px; margin-bottom: 8px; }
  .compact-deals .section-title { font-size: 18px; }
  .compact-deals .deal-count { font-size: 11px; padding: 0; background: transparent; color: var(--compact-muted); }
  .compact-deals #view-toggle { margin-left: auto; min-height: 36px; padding: 4px 0 4px 8px; border: 0; background: none; color: var(--compact-accent); font-size: 12px; cursor: pointer; white-space: nowrap; }
  .compact-deals #deals-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; }
  .compact-deals :is(.deal-card, .card) { min-width: 0; display: flex; flex-direction: column; min-height: 0; height: 100%; border-radius: 12px; border: 1px solid #303040; background: #1e1e2a; opacity: 1; animation: none; box-shadow: none; }
  .compact-deals :is(.card-image-wrap, .card-img, .image-placeholder) { width: 100%; height: 100px; min-height: 100px; padding: 8px; background: #fff; border-radius: 0; object-fit: contain; }
  .compact-deals .card-image-wrap img { object-fit: contain; border-radius: 0; }
  .compact-deals .card-body, .compact-deals .deal-card.no-img .card-body { padding: 8px; gap: 4px; min-width: 0; }
  .compact-deals .card-title { font-size: 13px; line-height: 1.35; font-weight: 600; -webkit-line-clamp: 2; overflow-wrap: anywhere; }
  .compact-deals .image-placeholder { display: flex; align-items: center; justify-content: center; flex-shrink: 0; background: #252533; color: #c4c4d9; font-size: 11px; }
  .compact-deals :is(.deal-card, .card):not(.no-img) > .image-placeholder { display: none; }
  .compact-deals .card-meta-row { display: flex; align-items: center; justify-content: space-between; gap: 4px; min-height: 14px; }
  .compact-deals .card-meta-row .cat-chip { margin: 0; line-height: 1.3; }
  .compact-deals .card-meta-row .card-meta { white-space: nowrap; line-height: 1.3; }
  .compact-deals .card-actions { display: flex; gap: 6px; align-items: center; margin-top: auto; min-height: 44px; }
  .compact-deals .card-actions .card-link { display: flex; align-items: center; flex: 1; width: auto; min-width: 0; min-height: 44px; padding: 0; font-size: 11.5px; line-height: 1.25; }
  .compact-deals .card-actions .share-btn { flex: 0 0 44px; width: 44px; height: 44px; padding: 0; font-size: 11px; display: flex; align-items: center; justify-content: center; }
  /* Offer-bearing titles must be fully visible, not hidden by a display clamp. */
  .compact-deals .has-conditions .card-title { display: block; overflow: visible; -webkit-line-clamp: unset; }
  .compact-deals .cat-chip { font-size: 10px; padding: 0; background: none; border: 0; color: var(--compact-muted); }
  .compact-deals .price-note { font-size: 11px; line-height: 1.35; color: var(--compact-muted); }
  .compact-deals .card-meta, .compact-deals .date { font-size: 10px; color: var(--compact-muted); }
  .compact-deals .card-footer { gap: 4px; padding: 0; min-height: 32px; }
  .compact-deals .share-btn { width: 36px; height: 32px; border: 0; color: var(--compact-accent); }
  .compact-deals .card-cta { display: block; width: 100%; padding: 7px 0; margin-top: 0; min-height: 32px; font-size: 12px; text-align: left; color: var(--compact-accent); background: none; border-radius: 0; text-shadow: none; }
  .compact-deals .discount-badge, .compact-deals .badge { background: var(--compact-solid); white-space: normal; }
  .compact-deals .promo-copy { position: relative; z-index: 1; min-height: 44px; padding: 6px; border: 1px solid #c4a2ef; border-radius: 6px; color: #eddbff; background: #2c1b3e; overflow-wrap: anywhere; cursor: pointer; font-size: 11px; }
  .compact-deals .feed-note { margin: 12px auto; color: var(--compact-muted); font-size: 12px; }
  .compact-deals .site-info { max-width: 1176px; margin: 0 auto 24px; padding: 0 12px; color: var(--compact-muted); font-size: .85rem; }
  .compact-deals .site-info summary { cursor: pointer; padding: 12px 0; }
  .compact-deals .hero-strip { background: none; border: 0; padding: 12px 0; }
  .compact-deals .hero-strip-title { color: var(--compact-accent); background: none; -webkit-text-fill-color: currentColor; white-space: normal; }
  .compact-deals .hero-strip-stat { display: block; }
  .compact-deals #deals-grid[data-view="list"] { grid-template-columns: minmax(0, 1fr); }
  .compact-deals #deals-grid[data-view="list"] :is(.deal-card, .card) { flex-direction: row; }
  .compact-deals #deals-grid[data-view="list"] :is(.card-image-wrap, .card-img, .image-placeholder) { width: 110px; flex-shrink: 0; height: 130px; }
  .compact-deals.category-page .wrap { max-width: 1200px; padding: 10px 12px 32px; }
  .compact-deals.category-page .crumb { font-size: 11px; margin-bottom: 6px; }
  .compact-deals.category-page .hero { padding: 0; border: 0; background: none; margin-bottom: 8px; }
  .compact-deals.category-page .hero h1 { font-size: 20px; line-height: 1.2; margin: 0; }
  .compact-deals.category-page .browse-controls { padding: 0; margin-bottom: 8px; }
  .compact-deals.category-page .category-picker { grid-column: 1 / -1; }
  .compact-deals.category-page .cat-pills { position: static; display: flex; flex-wrap: wrap; height: auto; padding: 0; margin: 0; background: none; border: 0; backdrop-filter: none; }
  .compact-deals.category-page .site-info { padding: 0; margin-top: 24px; }
  .compact-deals.category-page .card-foot { flex-wrap: wrap; }
  .compact-deals.category-page .disclaimer { color: var(--compact-muted); }
  @media (min-width: 640px) {
    .compact-deals .compact-header { padding: 0 24px; }
    .menu-label { display: inline; }
    .compact-deals :is(.card-image-wrap, .card-img, .image-placeholder) { height: 180px; min-height: 180px; }
    .compact-deals .card-title { font-size: 14px; }
    .compact-deals .price-note { font-size: 12px; }
    .compact-deals #deals-grid[data-view="list"] { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  }
  @media (min-width: 900px) { .compact-deals #deals-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); } }
  @media (min-width: 1200px) { .compact-deals #deals-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 16px; } }
  @media (max-width: 359px) { .compact-deals .deal-count { flex-basis: 100%; order: 3; } }
</style>
